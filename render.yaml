services:
  # The FastAPI Backend
  - type: web
    name: crm-hcp-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0 # Ensures modern Python version for compatibility
      - key: DATABASE_URL
        value: "postgresql://neondb_owner:npg_YIHTbpW4Fk2g@ep-old-boat-anq6obfl.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require"
      - key: GROQ_API_KEY
        sync: false # User will set this securely in the Render dashboard
